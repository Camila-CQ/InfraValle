plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.app_report"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17  // Cambiado a Java 17
        targetCompatibility = JavaVersion.VERSION_17  // Cambiado a Java 17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17 // Cambiado a Java 17
    }

    defaultConfig {
        applicationId = "com.example.app_report"
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            shrinkResources true
            minifyEnabled true
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
